# Slim serving subset — Streamlit RAG query path only. No GPU/ingestion deps.
# App
streamlit
python-dotenv
httpx
openai>=1.0
google-genai>=1.0
langchain
langchain-openai
langchain-google-genai
pyyaml>=6.0,<7.0
pydantic>=2.0

# Observability
langfuse>=4.0.0
opentelemetry-api

# Retrieval
pymongo[srv]
voyageai

# Graph runtime (the v4 extra, query-path members only)
langgraph>=1.1.2,<2.0.0
langgraph-checkpoint-sqlite>=2.0.0,<4.0.0
haystack-ai>=2.25.2,<3.0.0
mem0ai
