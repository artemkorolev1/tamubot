# Environment & secrets
.env
*.env.local
.mcp.json

# Python
.venv/
__pycache__/
*.py[cod]
*.pyd
*.egg
*.egg-info/
dist/
build/

# Test / lint / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Coverage
htmlcov/
.coverage
.coverage.*
coverage.xml

# Jupyter
.ipynb_checkpoints/

# Raw data (large files, regeneratable by running the scrapers)
tamu_data/raw/syllabi/
tamu_data/raw/catalog/
tamu_data/csce_syllabuses_no_url.zip

# Pipeline output (regeneratable)
data/syllabi/bronze/
data/syllabi/silver/

# PDFs anywhere in the repo (debug/test files)
*.pdf

# Large zip archives (source syllabi bundles — regeneratable)
tamu_data/processed/v3_step2_boilerplate.zip

# Processed syllabus data (regeneratable by running the ingestion pipeline)
tamu_data/processed/

# Logs
logs/

# Personal processing logs (progress checkpoints, error logs)
tamu_data/logs/

# LightRAG spike graph/vector storage (regeneratable by running ingest.py)
lightrag/storage/
lightrag/storage_*/
_archive/lightrag/storage*/

# Auto-generated timestamped eval reports and result dumps
tamu_data/evals/reports/eval_report_*.md
tamu_data/evals/reports/eval_results_*.jsonl
tamu_data/evals/reports/benchmark_*.md
tamu_data/evals/reports/benchmark_*.xlsx
# Keep: eval_analysis_*.md, eval_run_*.md (human-authored analysis docs)
# Keep: tamu_data/evals/golden_sets/ (test data — commit canonical sets)

# Debug output dumps (ad-hoc files generated during debugging sessions)
*_output.txt

# Streamlit
.streamlit/secrets.toml

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.bak

# Claude Code internal (keep skills — they're project-specific)
.claude/*
!.claude/skills/
.ccstatusline-settings.json
skills-lock.json

# auto-claude framework
.auto-claude/
.auto-claude-security.json

# Playwright MCP logs
.playwright-mcp/
.worktrees/

# One-off test scripts
test_hooks.sh
