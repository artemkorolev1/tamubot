services:
  dev:
    build: .
    # Launch Dagster UI (port 3000) in the background, then Streamlit (8501) as
    # the foreground/main process so both come up on every up/recreate/rebuild.
    command: 'bash -c "dagster dev -f src/tamubot/ingestion/pipeline_v6b/definitions.py --host 0.0.0.0 --port 3000 & exec streamlit run src/tamubot/app/streamlit.py --server.port 8501 --server.headless true"'
    volumes:
      - .:/workspace
      - /home/artem/.claude:/home/claude/.claude
      - /home/artem/.claude.json:/home/claude/.claude.json
      - /home/artem/.ssh:/home/claude/.ssh:ro
      - /home/artem/.gitconfig:/home/claude/.gitconfig:ro
      - /home/artem/.config/ccstatusline:/home/claude/.config/ccstatusline:ro
      - ./.docker-cache/pip:/home/claude/.cache/pip
      - ./.docker-cache/huggingface:/home/claude/.cache/huggingface
      - ./.docker-cache/torch:/home/claude/.cache/torch
      - dagster_home:/dagster_home
    env_file: .env
    ports:
      - "8501:8501"
      - "3000:3000"
      - "3001:3001"
    environment:
      - PYTHONPATH=/workspace:/workspace/src
      - TAMU_BASE_URL=http://api-proxy:8080/tamu
      - VOYAGE_BASE_URL=http://api-proxy:8080/voyage
      - PIP_USER=0
      # Postgres (pgvector) backend. Overrides any POSTGRES_URI in .env so the
      # container reaches the sibling service by name. RETRIEVAL_BACKEND stays
      # 'mongo' until the eval gate clears Postgres.
      - POSTGRES_URI=postgresql://tamubot:tamubot@postgres:5432/tamubot
    mem_limit: 22g
    cpus: "11"
    shm_size: "2g"
    deploy:
      resources:
        reservations:
          memory: 8g
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    depends_on:
      api-proxy:
        condition: service_started
      claude-perms:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    restart: unless-stopped
    stdin_open: true
    tty: true

  # Postgres + pgvector — Phase 1 retrieval backend. pg16 image ships pgvector.
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      - POSTGRES_USER=tamubot
      - POSTGRES_PASSWORD=tamubot
      - POSTGRES_DB=tamubot
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tamubot -d tamubot"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  # Heals Claude history dir permissions (root-created session dirs are
  # unreadable by the non-root 'claude' user) on every 'compose up'.
  claude-perms:
    image: busybox
    user: root
    volumes:
      - /home/artem/.claude:/claude
    command: ["sh", "-c", "mkdir -p /claude/projects/-workspace && find /claude/projects/-workspace -exec chmod a+rwx {} \\; ; echo claude-perms-done"]
    restart: "no"

  api-proxy:
    build: ./tools/api-proxy
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      - API_PROXY_TAMU_SESSION=1000
    volumes:
      - ./logs:/workspace/logs

  beszel:
    image: henrygd/beszel:latest
    restart: unless-stopped
    ports:
      - "8090:8090"
    volumes:
      - ./.beszel-data:/beszel_data

  beszel-agent:
    image: henrygd/beszel-agent:latest
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - LISTEN=45876
      - KEY=${BESZEL_KEY:-}

volumes:
  dagster_home:
  postgres_data:
